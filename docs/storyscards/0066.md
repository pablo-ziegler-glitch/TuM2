# [0066] Implementar carga de horarios (OWNER-06)

Estado: DONE  
Fecha de cierre: 2026-04-08  
PR: pendiente

## Alcance implementado

- Nueva pantalla real `OWNER-06` conectada en router:
  - Ruta `/owner/schedules` ahora abre `OwnerScheduleScreen`.
  - Reemplazo del placeholder previo.
- UI completa de horarios OWNER alineada a Stitch 0066:
  - estado inicial vacío con CTA `Configurar horarios`,
  - edición semanal por día con modalidad `Cerrado / Horario corrido / Horario cortado`,
  - validaciones inline por día (formato, orden, solapamientos),
  - vista previa persistente en barra inferior (`Se verá como`),
  - guardado con feedback de éxito/error,
  - guard de cambios sin guardar al salir.
- Sección de excepciones y cierres:
  - alta/edición/eliminación de fecha especial,
  - alta/edición/eliminación de cierre temporal por rango,
  - estado vacío de excepciones con CTAs.
- Persistencia Firestore (OWNER privado):
  - `merchants/{merchantId}/schedule_config/weekly`,
  - `merchants/{merchantId}/schedule_exceptions/{yyyy-mm-dd}`,
  - `merchants/{merchantId}/schedule_exceptions_ranges/{rangeId}`.
- Backend Functions:
  - nuevos triggers sobre weekly, excepciones y rangos,
  - helper único de recompute `recomputeMerchantOperationalProjection(merchantId)`,
  - actualización derivada en `merchant_operational_signals` y `merchant_public`.
- Firestore Rules:
  - permisos por ownership/admin para subcolecciones nuevas de horarios.
- Feature flag:
  - integración real de Remote Config con `owner_schedule_editor_enabled`,
  - fallback seguro y pantalla de módulo deshabilitado para rollback visual sin redeploy.
- Analytics OWNER-06:
  - `owner_schedule_screen_view`,
  - `owner_schedule_mode_selected`,
  - `owner_schedule_apply_weekdays_template`,
  - `owner_schedule_add_exception`,
  - `owner_schedule_save_success`,
  - `owner_schedule_save_error`,
  - `owner_schedule_validation_error`.

## Tests y validación ejecutada

- `flutter analyze --no-fatal-infos lib/modules/owner/screens/owner_schedule_screen.dart lib/modules/owner/schedules lib/core/router/app_router.dart test/modules/owner/schedule_validation_test.dart`
- `flutter test test/modules/owner/schedule_validation_test.dart`
- `cd functions && npm run build`
- `cd functions && npm test`

Resultado: PASS.

## Notas

- El flujo quedó preparado para evolución de validación extra y analytics de OWNER-06.
- El cálculo de `opensNextAt` backend considera weekly + precedencia diaria (rango > excepción > weekly).
